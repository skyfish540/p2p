<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.bjpowernode.p2p"/>
    <!--开启SpringMVC对注解的支持否则部分注解无法使用例如
        @ResponseBody
     -->
    <mvc:annotation-driven/>
    <!--配置 对静态资源文件的访问-->
    <mvc:default-servlet-handler/>

    <!--定义视图解析器-->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
    <!--定义拦截器类-->
    <!--<mvc:interceptors>
        &lt;!&ndash;可以同时配置多个拦截器去拦截同一个用户请求这时拦截器会按照配置的顺序继续执行，先配置的先执行后配置的后执行 &ndash;&gt;
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <bean class="com.bjpowernode.rbac.interceptor.UserInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>-->
</beans>